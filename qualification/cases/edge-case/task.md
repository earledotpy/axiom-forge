# Edge-case qualification task

Add a `greeting(name)` function to `qualification/fixture/message.py`.

For a non-empty name, it must return `Hello, <name>!`. For an empty string,
it must return this exact value:

Hello, stranger!

Keep the existing `message()` function unchanged.
