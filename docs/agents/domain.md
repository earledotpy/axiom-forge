# Domain Docs

This is a single-context repository. Read the root `CONTEXT.md` and relevant ADRs in `docs/adr/` before planning or implementing a change.

Use the glossary vocabulary in issues and implementation documents. Surface any conflict with an ADR instead of silently overriding it.
