# Keep retry operator-directed

The first local operator workbench will not automatically retry a failed captured run with another adapter. It may present the failed evidence summary, adapter availability hints, and a confirmed retry action for the operator to choose, preserving the original multi-agent continuity goal without turning Forge into an autonomous quota router or failover orchestrator.
