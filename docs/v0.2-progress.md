# v0.2 Progress

This note records completed, verified v0.2 work; it is not a roadmap.

## Completed

### 2026-06-19 — Enforce run directory identity

- Commit: `27f3b0d16f4b21f9aaf9768ab1cbaee706b7099a`
- `record.json.run_id` must exactly match its run-directory name.
- Validation fails closed with `run_id_directory_mismatch` when the values differ.
- Promotion regression covers direct validation rejection, promotion rejection, and no gate branch for a mismatched record ID.
- `forge_check.sh` passed on `main`.

### 2026-06-20 — Make promotion policy config truthful

- Commit: `cf42ec16c018023a3c0d9721defa0c97a9893a54`
- Removed misleading promotion switches for clean target, operator approval, existing gate branches, and current base.
- Current-base rejection is an unconditional `stale_base_sha` check.
- Promotion regression covers a valid-but-stale base, the recorded `stale_base_sha` reason, and no gate-branch creation.
- `forge_check.sh` passed on `main`.

### 2026-06-21 — Detect parent dirtiness after adapter

- Commit: `367129aecdab19622aa80080deb48ffe449ac132`
- The runner fails an adapter run with `parent_repo_dirty_after_adapter` if the parent repository working tree is dirty after adapter return.
- The run is not recorded as `COMPLETED` in that condition.
- The runner test cleans up the `.axiom-parent-dirty-test` sentinel.
- `tests/runner/run_all.sh`, `tests/promote/run_all.sh`, and `forge_check.sh` passed on `main`.

### 2026-06-21 — Detect target-checkout writes outside the disposable worktree

- The runner now snapshots target-checkout Git status, including ignored paths, before and after adapter execution.
- Any change fails closed with `adapter_modified_outside_worktree` and produces a failed `record.json`.
- Runner case R2d uses `bad-outside-worktree-agent` to write an ignored file in the parent checkout; this proves the new check detects a write that ordinary `git status --porcelain` omits.
- This remains detection of target-checkout mutations, not OS-level confinement or a claim to observe arbitrary host-path writes.

### 2026-06-21 — Document real CLI adapter evidence format

- `docs/adapter-evidence.md` now defines required fields for future real CLI adapter evidence.
- The format separates observed CLI metadata from verified run and promotion outcomes.
- CLI availability or version evidence does not automatically change adapter trust or status.
- Existing historical evidence is not run-bound version evidence unless it explicitly says so.
- Antigravity remains experimental unless separately reviewed and approved through future evidence and an explicit human decision.

## Deferred Candidate

### Non-recursive gate-regression verification

- Axiom Forge self-changes are currently verified during promotion by the configured unit command, not by the full runner and promotion matrices.
- Directly requiring `scripts/forge_check.sh` during promotion is unsafe or recursive: `verify_patch.sh` applies candidate patches in an uncommitted disposable worktree while `forge_check.sh` requires a clean worktree, and `forge_check.sh` runs promotion tests that invoke `promote.sh` and re-enter verification.
- No narrow implementation slice is approved for v0.2 at this time.
- Future work requires a separately designed non-recursive gate-contract test architecture.
- Existing `forge_check.sh` remains the full manual health proof.

## Scope Boundary

This note records completed work only. It does not declare v0.2 complete or released.
